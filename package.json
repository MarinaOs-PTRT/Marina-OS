{
  "name": "marina-os",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema Operativo per Porto Turistico Riva di Traiano",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm -w apps/web run dev",
    "build": "npm -w apps/web run build",
    "dev:web": "npm -w apps/web run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
